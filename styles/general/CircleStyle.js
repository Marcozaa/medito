import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  circle: {
    width: 150,
    height: 150,
    position: "absolute",
    borderRadius: "100%",
  },
});

export default styles;
